#pragma once

// Prints usage
void print_usage();

// Prints invalid args error message
void print_invalid_args();

// Prints invalid filename error message
void print_invalid_filename();

// Prints invalid timer_seconds error message
void print_invalid_timer_seconds();

// Prints an error message if the name of the file is too long 
void print_name_too_long();

// Prints an error message if the file already exists
void print_file_already_exists();